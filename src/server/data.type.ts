export type ContactFormData = {
  email: string;
  firstName: string;
  lastName: string;
  message: string;
};

export type ProductsFormData = {
  email: string;
  firstName: string;
  lastName: string;
  plan: string;
  date: Date;
  location: string;
  additionalInformations?: string;
};
